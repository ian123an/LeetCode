# Write your MySQL query statement below
SELECT DISTINCT num AS ConsecutiveNums
FROM (
    SELECT id, num, LAG(num) OVER(ORDER BY id) AS old_num, LEAD(num) OVER(ORDER BY id) AS new_num
    FROM Logs
) AS consecutively
WHERE num = old_num AND old_num = new_num;
